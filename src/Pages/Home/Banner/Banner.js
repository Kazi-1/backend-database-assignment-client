import React from 'react';
import { Button, FormControl, InputGroup } from 'react-bootstrap';
import './Banner.css'

const Banner = () => {
    return (
        <div className="banner-img">
            {/* <h1 className="fw-bold">Time to order food</h1>
            <p className="fw-bold">Find restaurants in your area now</p>
            <InputGroup className="mb-3 w-50 mx-auto">
                <FormControl
                    placeholder="Your Street and House number"
                    aria-label="Recipient's username"
                    aria-describedby="basic-addon2"
                />
                <Button className="bg-warning text-light border-0" variant="outline-secondary" id="button-addon2">
                    FIND RESTAURANTS
                </Button>
            </InputGroup> */}

        </div>
    );
};

export default Banner;